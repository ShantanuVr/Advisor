# Personal Advisor Portal Configuration
# No API keys needed - uses Cursor-in-the-loop workflow

# Timezone for display and scheduling
TIMEZONE=America/New_York

# Data directory (relative to project root)
DATA_DIR=./data

# News sources to fetch (comma-separated)
NEWS_SOURCES=fed_official,reuters

# Danger window around high-impact events (minutes)
DANGER_WINDOW_MINUTES=30

# Server settings
HOST=127.0.0.1
PORT=8000
